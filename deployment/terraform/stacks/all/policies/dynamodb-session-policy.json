{
  "_comment": "Policy rationale: optional — only needed when BA_SESSION_STORE_TYPE=dynamodb. Scopes table access to any table in this account/region so the blog stack's DynamoDB session store (if chosen) can read/write session items. Account ID and region are templated at apply time.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "dynamodb:GetItem",
        "dynamodb:PutItem",
        "dynamodb:UpdateItem",
        "dynamodb:DeleteItem",
        "dynamodb:Query"
      ],
      "Resource": "arn:aws:dynamodb:${region}:${account_id}:table/*"
    }
  ]
}
