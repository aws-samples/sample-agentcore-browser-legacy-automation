{
  "_comment": "Policy rationale: CRUD + Query access on the DynamoDB session table for session-store persistence when BA_SESSION_STORE_TYPE=dynamodb. Resource ARN wildcarded on region and account because the runtime assumes the role in whatever region it runs; scope further if you pin a specific table name.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "BrowserSessionTable",
      "Effect": "Allow",
      "Action": [
        "dynamodb:GetItem",
        "dynamodb:PutItem",
        "dynamodb:UpdateItem",
        "dynamodb:DeleteItem",
        "dynamodb:Query"
      ],
      "Resource": "arn:aws:dynamodb:*:*:table/*"
    }
  ]
}
